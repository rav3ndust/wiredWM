# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# - - - this is sway specific: - - -
# XXX it is also being tested, so comment it out when not using it. 
#exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP=sway XDG_SESSION_DESKTOP=sway
exec systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK
exec swaybg -i $HOME/wiredWM/wp/lain-twin.png &
#exec killall swaybar; waybar &

# make windows float by default.
# comment/uncomment the below line to disable/enable tiling by default.
#for_window [class="[.]*"] floating enable

# for window title bars
for_window [class="^.*"] border pixel 4 

# for gaps
#####################################
# - - smart gaps/borders.
smart_gaps on
smart_borders on
# - - manual gaps.
# You can manually adjust their values below, if you choose to set them.
#gaps inner 7
#gaps outer 5 

#for_window [class=".*"] border pixel 2 gaps inner 50 gaps outer 20 

# for border colors
# the final hex code is window border. currently = Neon Pink (#FF10F0)
client.focused 		#ff10f0   #000000    #39FF14  #39FF14  #FF10F0 
client.unfocused 	#000000   #000000    #ff3131
#              		border   background   text     indic   child_border
##############################################################################   
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:Ubuntu 10
#font pango:Cantarell 10
#font pango:Inter 10
font pango:Quicksand 10
#font pango:Poppins 10
##############################################################################
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock-fancy --nofork

# volumeicon provides us a simple, GUI way to control volume with mouse
# (volumeicon on arch/ubuntu/deb.)
# (if using pipewire, use pasystray.)
exec --no-startup-id volumeicon
# ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
# polybar has built in volume controls. don't worry about the above if using polybar.

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Blueman (bluetooth-manager) is used to handle Bluetooth connections.
# We use `blueman-applet` to display the BT status in the bar.
exec --no-startup-id blueman-applet

# we currently use dunst to handle system notifications.
# make sure it starts on launch, because apps that rely on notifications might hang.
exec --no-startup-id dunst

# we use nitrogen for wallpaper management.
# autostart nitrogen for the wallpaper.
# we should make a command for a default wallpaper.
exec --no-startup-id nitrogen --restore

# we use `meteo-qt` for keeping tabs on the weather.
# it displays a small widget showing weather in the panel.
# you can click the widget to pop out a window with forecast.
# You can edit your City and the widget by right-clicking it, and selecting 'settings.'
exec --no-startup-id meteo-qt

# we use Flameshot for easily taking screenshots from the panel.
exec --no-startup-id flameshot

# we use lxpolkit to handle priv escalation when needed.
exec --no-startup-id lxpolkit

# start compositor. we use xcompmgr
exec --no-startup-id xcompmgr

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status && notify-send "wired helper" "volume +10"
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status && notify-send "wired helper" "volume -10"
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status && notify-send "wired helper" "volume muted" 
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal.
# Our installed terminal emulators are "st", "gnome-terminal", "konsole", "kitty" and "alacritty."
# Choose which one works best for you!
bindsym $mod+Return exec st
#bindsym $mod+Return exec kitty
#bindsym $mod+Return exec alacritty
#bindsym $mod+Return exec gnome-terminal
#bindsym $mod+Return exec konsole

# kill focused window
bindsym $mod+Shift+q kill

# start applications and run commands (your choice of dmenu or rofi)
# start dmenu (a program launcher) - nightshade customized dmenu
bindsym $mod+d exec dmenu_run -fn "Quicksand-10" -nb black -nf pink -sb green -sf red -p "nightshade - run command: " 
# rofi version of the above
#bindsym $mod+d exec --no-startup-id "rofi -show run -theme purple -font 'Quicksand Bold 10'"
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
bindsym $mod+Shift+a exec --no-startup-id i3-dmenu-desktop --dmenu='dmenu -fn "Quicksand-10" -i -nb black -nf pink -sb green -sf red -p "nightshade - Applications List: "' 
# rofi version of the above.
#bindsym $mod+Shift+a exec --no-startup-id "rofi -show drun -theme purple -font 'Quicksand Bold 10'"

#optional alternative: 
# i3-dmenu-desktop (rofi)...
#bindsym $mod+Shift+a exec --no-startup-id "i3-dmenu-desktop --dmenu='rofi -dmenu'"

# launch rofi - autorun, ssh, app picker.
bindsym $mod+Shift+d exec --no-startup-id "rofi -combi -modi window,drun,run -theme purple -font 'Quicksand Bold 10' -show combi"
######################################################################
# add a shortcut for displaying recent notifications
bindsym $mod+Shift+n exec --no-startup-id "dunstctl history-pop" 

# add a shortcut for popping the calendar
bindsym $mod+c exec --no-startup-id "gsimplecal"

# add a popup notification showing when Caps_Lock has been pressed
bindsym Caps_Lock exec --no-startup-id "notify-send 'wired helper' 'Caps Lock pressed.'"

# add a keybinding for quick screenshots (currently using `flameshot gui` for this functionality
bindsym $mod+p exec --no-startup-id "flameshot gui"

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# quickly lock the display. we use `i3lock-fancy` for this.
bindsym $mod+Shift+z exec "i3lock-fancy"
# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Exit command detected. Are you sure you would like to logout of wiredWM? This X session will be closed.' -B 'Yes, log out of wiredWM' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# you can uncomment this if you want to replace i3bar with polybar.
# exec --no-startup-id polybar

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	# uncomment the below if we want i3bar on top.
	position top	
    	status_command i3status
	# font choices we like - Ubuntu, Inter, Noto Sans, and Cantarell.
	# both are nice and readable in wired.
	# use the font of your choice by commenting/uncommenting them.
	#font pango:Ubuntu 10
	#font pango:Cantarell 10
	#font pango:Inter 10
	#font pango:ubuntu-title 11 
	font pango:Quicksand Bold 10 
	#font pango:Poppins 10
	colors {
		focused_workspace #ff10f0 #000000 #39FF14
		inactive_workspace #000000 #000000 #ff3131
		separator #ff10f0
		statusline #ff10f0 
	}
}

# custom conky displaying time, a message, and date in US format:
exec --no-startup-id conky

# we're using xautolock+i3lock to lock the screen after 10 mins of inactivity. The screen will blank and you'll be prompted for your password.
# you can change the lock time by editing the number behind the -time flag. You can also edit the color of the lock screen using hexadecimal. Right now, it is set to purple (6a0dad)
#exec "xautolock -detectsleep -time 10 -locker \"i3 lock -c 6a0dad --show-failed-attempts\""
# We're using xscreensaver to handle screensaving and autolock.
#exec --no-startup-id xscreensaver

# we want to start the gnome-keyring-daemon so we can store secrets for browsers and other applications.
exec --no-startup-id /usr/bin/gnome-keyring-daemon --start --components=secrets

# ensure proper kb layout
exec "setxkbmap -layout us,de"
###########################################################
# XXX  - - - Sway-specific Things - - - 
# These are items that are specific to Sway. Keep them separate from i3-specific things to avoid confusion.
###########################################################
default_border pixel 3

# XXX: 	- - - Special Section For Local Machine - - - 
# You can use this section for special commands you might need to run on your local machine.
# For uniformity, please don't commit anything below this line to the public repo.
###################################
